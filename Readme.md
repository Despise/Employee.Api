# Employees API Net Core 3.1

+ Entity Framework
+ Swagger
+ AutoMapper
+ Docker

## Docker comandos

* docker build -t employees-api .
* docker images
* docker run -d -p 8080:80 --name employees-api employees-api